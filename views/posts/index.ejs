<% include ../_header %>

  <% posts.forEach((post) => { %>
  <%= moment(post.createdAt).format('MMM YYYY') %>
  <a href="<%='posts/' + post.id %>">
    <%= post.title %>
  </a> 
  <p>
    <%= post.preview %>
  </p>
  <br />
  <% }) %>

<% include ../_footer %>


